{
  "name": "modsuav",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./src/style.css -o ./_site/style.css && npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.4.1"
  }
}
